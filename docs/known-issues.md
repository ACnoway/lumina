# 已知问题与技术债务

> 记录开发过程中发现但暂不修复的 bug、限制和技术债务。
> 每条记录格式：模块 → 问题描述 → 影响 → 建议方案 → 优先级。

---

## 平台货币模块

### CURRENCY-001 · 支付渠道真实商户回调尚未完成联调（代码已实现）

- **范围**：平台消费、钱包、账本和模型价格已经统一使用光子；后台可配置 `1 人民币 = N 光子`。
- **当前行为**：支付订单会保存人民币金额、创建时汇率和光子金额；用户从前台选择具体渠道，经过回调验签或主动查询后调用 `WalletService.recharge()`。
- **限制**：易支付、支付宝和微信 Adapter 已实现，但当前没有真实商户沙箱、公网回调和本地完整运行环境，暂不能标记真实支付回调已验收。
- **当前验证**：提交 `50536c4` 已在 `lch:/root/lumina` 完成 Prisma Client 生成、根级 lint/test/build，并通过隔离 Compose 冒烟；现有冒烟未覆盖真实支付下单、假回调、重复回调、金额攻击或退款。
- **后续**：在远程配置测试商户或各渠道沙箱凭据后，补做用户选择具体 `channelId`、订单幂等、签名回调、重复回调、金额/币种校验、主动查询和退款联调；需要公网回调时再补受控的回调入口验证。
- **优先级**：P1

### PAYMENT-001 · 易支付回调方式与查单兼容问题导致充值不入账（已修复，远程验证通过）

- **发现时间**：2026-09-19
- **问题**：用户完成易支付付款后，支付平台向 `/api/payments/notify/:channelId` 发起 GET 异步通知；后端此前只注册了 POST 路由，Nginx 日志记录该回调返回 404。进一步排查发现，旧版易支付 `api.php` 查单要求 `key` 参数，原 Adapter 却发送了 `sign`，无法通过主动查单兜底。
- **影响**：订单停留在 `PENDING`，`paidAt` 为空，没有生成回调事件，也不会调用 `WalletService.recharge()`，前台只能看到支付平台回跳参数，余额不变。
- **证据**：订单 `LM20260919043915205578276370` 创建成功，支付平台回跳带有 `trade_status=TRADE_SUCCESS`；远程日志显示 GET 回调 404，数据库确认该订单为 `PENDING` 且回调事件数为 0。
- **修复**：支付通知控制器同时支持 GET 和 POST；GET 请求将查询参数交给渠道 Adapter 验签；GET 验签失败时仅通过商户 key 向易支付查单，确认渠道返回成功/关闭后才继续处理；修正 `api.php` 查单参数；前台等待中的订单主动调用同步接口。
- **验证结果**：本地支付控制器/易支付 Adapter 单测 5 个通过，后端全量测试 21 个套件/92 个测试通过；远程拉取 `a519dbc` 并重建 backend/frontend 后，重放原始 GET 通知返回 `success`，订单 `LM20260919043915205578276370` 变为 `SUCCEEDED`，回调事件 `processed=true`，钱包产生 `RECHARGE` 1 光子交易，幂等键为 `payment:recharge:LM20260919043915205578276370`。
- **优先级**：P0
- **状态**：已修复并在远程真实支付订单上验证；支付宝官方、微信支付 API v3 仍需各自沙箱凭据分别联调。

### PAYMENT-002 · 查单接口成功码被误判为支付成功（已修复，远程验证通过）

- **发现时间**：2026-09-19
- **问题**：前端订单进入 `PENDING` 后自动调用主动同步；易支付 Adapter 将查单响应的 `code=1` 直接映射为 `SUCCESS`。部分兼容网关中该字段只表示查单请求成功，未付款订单也会返回，导致首次轮询就调用 `WalletService.recharge()`。
- **额外风险**：易支付 GET 回调验签失败时会走商户密钥查单兜底；此前这类事件仍被记录为 `signatureValid=true`，审计语义不准确。
- **修复**：查单只接受明确的已支付状态，未知/未支付状态保持 `PENDING`；成功入账强制校验金额和币种；订单已关闭或失败后不得恢复入账；前端轮询改为只读订单状态；查单兜底事件标记为非签名回调。
- **回归范围**：覆盖未付款查单、缺失状态、明确成功查单、重复/并发回调、无效签名、金额/币种不符和公开回调 URL。
- **优先级**：P0
- **状态**：代码与文档已更新；提交 `550dcbb` 已在 `lch:/root/lumina` 通过 pnpm 8.15.0 全量测试（22 个测试套件 / 99 个测试）、生产构建和隔离 Compose 冒烟，测试资源已清理。现有 smoke 未覆盖真实支付渠道交易，受影响真实易支付渠道重新启用前仍需完成商户沙箱/回调验证，并对历史订单逐笔对账。

## 认证模块（Auth）

### A-001 · 邮箱验证码发送依赖 SMTP，失败时接口返回 400（代码已修复，真实 SMTP 验收待补）

- **发现时间**：2026-09-16（现状核对）
- **文件**：`apps/backend/src/modules/auth/auth.service.ts` → `sendCode()`
- **历史现象**：浏览器调用 `POST /api/auth/send-code` 收到 HTTP 400。前端通过 Next.js rewrite 转发到 `POST /auth/send-code`，路由本身存在；服务在 `transporter.sendMail()` 失败时显式抛出 `BadRequestException('邮件发送失败，请稍后重试')`。
- **当前环境证据**：仓库没有本地 `.env`，本机也没有启动后端、Redis、PostgreSQL 或 MinIO。`.env.example` 和 Docker Compose 的默认 SMTP 主机为 `smtp.example.com`，仅是示例配置，无法投递真实邮件。因此当前登录不能标记为可用；本次未连接真实 SMTP，不能断言具体供应商错误。
- **额外缺陷（已修复）**：验证码此前在调用 `sendMail()` 前就被写入 Redis，邮件发送失败后 key 未清理；限频提示也把剩余 TTL 错当成分钟。现改为独立 Redis 冷却 key 原子限频，仅在投递成功后保存验证码，失败清理验证码和冷却状态，并按秒提示。
- **影响**：未配置或配置错误的 SMTP 会导致所有新用户和已注册用户无法登录；第一次投递失败后，短时间内无法立即重试。
- **已完成**：仅在成功投递后保存验证码；使用独立的 60 秒 `setNX` 冷却 key；投递失败清理验证码和冷却状态；统一邮箱规范化和剩余秒数提示；增加 mock SMTP 的成功、失败清理、失败重试、并发限频、一次性使用和登录失败测试。
- **后续建议**：启动时校验 SMTP 必填配置，部署前使用 `transporter.verify()` 做健康检查；在 MailHog 或真实 SMTP 可用后补 API/E2E 验收。
- **优先级**：P0
- **状态**：代码已修复（2026-09-16），待配置真实 SMTP 并完成端到端验证。

### A-002 · 认证、聊天和生图缺少端到端测试（最小 API 旅程已通过）

- **发现时间**：2026-09-16（现状核对）
- **问题**：现有 15 个单元测试套件、59 个测试覆盖核心服务、计费校验和供应商配置校验；此前没有认证邮件、JWT 登录、聊天 SSE、图片任务、MinIO 上传或真实数据库/Redis 的 API/E2E 测试。
- **影响**：模块可构建和单元测试通过不代表配置、网络、第三方服务和真实请求链路可用；认证、聊天、生图和管理端仍缺少完整 E2E。
- **建议方案**：使用新增的 `docker-compose.e2e.yml` 和 `tests/e2e/smoke.mjs`，在远程 Docker 环境中执行登录到聊天/生图的最小用户旅程；真实供应商再单独做受控冒烟测试。
- **优先级**：P0
- **状态**：测试环境与最小冒烟脚本已实现；2026-09-17 在 `lch:/root/lumina` 通过认证、RBAC、管理员用户状态启停/余额账本/审计、聊天 SSE、生图队列/MinIO、历史和管理配置最小旅程。真实供应商和浏览器 E2E 仍待补。

### A-003 · 根级验证受 pnpm 版本不一致阻塞

- **发现时间**：2026-09-16（现状核对）
- **问题**：项目声明 `packageManager: pnpm@8.15.0`，但当前环境为 pnpm 11.19.0。执行根级 `pnpm lint`、`pnpm test`、`pnpm build` 时，pnpm 在依赖目录检查阶段要求清理不兼容的 node_modules，并在非交互会话中中止。
- **影响**：无法以仓库定义的根脚本完成当前基线验证；CI 与本地的工具链结果也可能不一致。
- **建议方案**：统一开发机和 CI 使用 pnpm 8.15.0，先以干净安装执行 `pnpm install --frozen-lockfile`；同时把 lint 拆分为只检查的 `lint` 和显式修复的 `lint:fix`，避免验证命令改写源文件。
- **优先级**：P1
- **修复时间**：2026-09-17
- **修复方式**：根级、backend 和 frontend 新增显式 `lint:fix`；`lint` 改为只读检查。
  backend 的 Prettier 规则暂降为 warning，避免历史格式债务触发自动改写或阻断 CI；
  Provider 脱敏响应改为复制配置后删除 `apiKey`，并修复了实际的未使用变量错误。
- **验证**：`lch:/root/lumina` 使用 pnpm 8.15.0 执行根级 lint、test、build 通过；lint
  无 error 但有 238 个历史 warning。pnpm 11 的本地环境仍未统一。
- **状态**：部分修复；依赖工具链统一和历史格式清理仍待完成。

---

## 钱包模块（Wallet）

### W-001 · preDeduct 并发安全非原子操作（已修复）

- **发现时间**：2026-08-14（第三步开发）
- **文件**：`apps/backend/src/modules/wallet/wallet.service.ts` → `preDeduct()`
- **问题**：预扣流程中「检查余额」和「写入 Redis 预扣记录」是两步非原子操作。当前代码先 `redis.get()` 检查幂等键是否存在，再 `redis.set()` 写入。高并发下同一用户可能绕过检查重复预扣，导致锁定金额超过实际余额。
- **影响**：单用户高并发请求场景下可能超额锁定。当前阶段用户量低、预扣金额有上限，实际风险较小。
- **建议方案**：在 `RedisService` 中暴露 `setNX(key, value, ttl)` 方法，使用 Redis `SET key value NX EX <ttl>` 原子命令替代当前的 get-then-set 两步操作。同时可考虑用 Redis 分布式锁（`SET lock:wallet:{userId} NX EX`）保护余额检查+预扣写入整个流程。
- **修复时间**：2026-09-16
- **修复方式**：使用用户级 Redis 分布式锁包住幂等检查、余额检查和 reservation 写入，并用 Redis transaction 原子写入预扣记录及用户 reservation 索引。
- **验证**：100 个相同幂等键并发请求最终只创建一条 reservation。
- **状态**：已修复

### W-002 · 余额检查与预扣写入之间的竞态（已修复）

- **发现时间**：2026-08-14（第三步开发）
- **文件**：`apps/backend/src/modules/wallet/wallet.service.ts` → `preDeduct()`
- **问题**：`preDeduct` 先从数据库读取余额判断是否足够，再写入 Redis 锁定。两步之间余额可能被其他请求扣减，导致预扣时认为余额足够但实际结算时不足。结算阶段有 Prisma 事务保护会抛出 `BadRequestException`，不会造成数据不一致，但用户体验不够友好。
- **影响**：极端并发下用户可能预扣成功但结算失败，需要退回并重试。
- **建议方案**：与 W-001 一并解决，使用 Redis 分布式锁或数据库行级锁（`SELECT ... FOR UPDATE`）保护预扣流程。
- **修复时间**：2026-09-16
- **修复方式**：所有余额变更操作使用同一用户级 Redis 锁；settle 额外使用数据库 `balance >= amount` 条件更新，作为锁失效或绕过应用锁时的最终资金安全边界。
- **验证**：100 个不同幂等键并发预扣时，reservation 总额不超过钱包余额。
- **状态**：已修复

---

## 供应商模块（Providers）

### P-001 · 限流使用固定窗口而非滑动窗口（已修复）

- **发现时间**：2026-08-14（第四步开发）
- **文件**：`apps/backend/src/modules/providers/providers.service.ts` → `checkRateLimit()`
- **历史问题**：限流使用 Redis INCR + EXPIRE 实现固定窗口计数（按分钟）。在窗口临界点（如 59 秒到 00 秒），可能短暂出现接近 2 倍限流值的流量。
- **影响**：高并发场景下固定窗口会造成突发流量超过配置额度。
- **修复方案**：改为 Redis ZSET 滑动窗口，并通过 Lua 脚本原子完成服务端取时、过期成员清理、当前窗口计数、配额写入和 TTL 更新；限流 key 不再包含分钟编号，多实例共享同一滚动窗口。
- **优先级**：低
- **验证**：新增 Redis/Provider 限流单测；提交 `3a61940` 已在 `lch:/root/lumina` 的隔离 PostgreSQL/Redis 环境通过完整 smoke，限额为 1 时首个聊天请求成功、同窗口第二个请求返回 503。
- **状态**：已修复（2026-09-17）

---

## 生图队列模块（Image Queue）

### I-002 · 提示词优化模型此前只能通过环境变量配置

- **发现时间**：2026-09-17（功能核对）
- **历史现象**：`POST /image/optimize-prompt` 固定读取 `PROMPT_OPTIMIZER_MODEL`，管理后台没有模型选择入口；默认模型未配置或没有上游时，生图页面的优化按钮无法真正完成调用。
- **修复方式**：增加 `system_configs` 配置表和管理员读写接口，在后台从已有且可用的 `CHAT` 平台模型中选择；优化流程复用现有聊天 Provider/Adapter 和实际 token 计费，环境变量仅保留为兼容兜底。
- **验证**：新增 Settings、Admin 和 ImageService 单测，并在远程 mock Provider E2E 中验证管理员配置、普通用户调用、RBAC 和审计记录。
- **优先级**：P1
- **状态**：已修复（2026-09-17）

### I-001 · 上游生图请求在进程崩溃后的恢复中可能重复执行

- **发现时间**：2026-09-16（持久化队列实现）
- **文件**：`apps/backend/src/modules/image/image.service.ts` → `recoverQueuedTasks()`
- **现象**：worker 在上游已经完成生成、但本地尚未写入任务成功状态时异常退出，任务会在 15 分钟后被视为过期并重新处理。
- **影响**：账本使用固定 `image:<taskId>` 幂等键，不会重复扣费；若上游不识别幂等键，恢复任务仍可能再次产生上游生成成本或重复图片。
- **修复方式**：OpenAI Images、OpenAI-compatible 和 Stability Image 请求统一传递稳定的 `Idempotency-Key: image:<taskId>`；E2E mock Provider 会按该键复用响应，避免测试重试时生成不同结果。
- **剩余边界**：真实供应商是否真正执行幂等由其 API 契约决定；后续若拆分独立 worker，仍可增加任务 lease/heartbeat 和显式的“上游已接受”状态，进一步缩小恢复窗口。
- **优先级**：P1
- **状态**：已缓解（2026-09-17）；上游幂等能力需按实际供应商单独确认。

---

## 已修复的问题

> 以下问题已在开发过程中修复，保留记录用于回顾。

### W-000 · settle 方法异常被 try-catch 吞掉（已修复）

- **发现时间**：2026-08-14（第三步开发）
- **文件**：`apps/backend/src/modules/wallet/wallet.service.ts` → `settle()`
- **问题**：subagent 初版代码在 `settle()` 外层包了 try-catch，将事务内抛出的 `BadRequestException`（余额不足）和 `NotFoundException`（钱包不存在）统一转换为 `InternalServerErrorException`（500 错误），前端无法获得正确的错误状态码。
- **修复方式**：移除外层 try-catch，让 NestJS 内置异常正常传递。事务内的 `BadRequestException` 和 `NotFoundException` 会自动映射为 400/404 响应。
- **状态**：已修复（2026-08-14）
